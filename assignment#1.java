class GratvityCalculator {
    public static void main(string[] args){
        double fallingPosition;
        double gravity = -9.8;
        double initialVelocity = 0.0;
        double fallingTime = 10.0;
        double initialPosition = 0.0;
        fallingPosition = (0.5 * gravity * (fallingTime * fallingTime) + (initialVilocity * fallingTime) + initialPosition);
        System.out.println(x);
    }

}